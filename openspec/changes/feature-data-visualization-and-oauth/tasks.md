## 1. Analytics & Visual Execution

- [x] 1.1 Run `npm install recharts` directly in the project terminal.
- [x] 1.2 Refactor the dashboard data layer extracting an array specifically segmenting `academic_tasks` into "Concluídas" and "Pendentes".
- [x] 1.3 Configure a localized Recharts `<PieChart>` visual Donut integration, mapping its arcs natively onto Metallic Blue hex constraints securely housed within standard High-End UI Glassmorphism bounds.

## 2. Authentication Federation Protocol

- [x] 2.1 Refactor the `app/auth/page.jsx` visually adding a PT-BR designated "Login com Google" button immediately alongside existing email actions.
- [x] 2.2 Wire the specific `signInWithOAuth({ provider: 'google' })` operational sequence securely within the `@supabase/supabase-js` auth handlers tracking click logic natively.
