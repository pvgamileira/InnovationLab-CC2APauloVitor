## 1. Database Implementation

- [x] 1.1 Create the new SQL migration script to construct the `academic_tasks` table mapped properly to `subjects` and `user_id`.
- [x] 1.2 Integrate precise Row Level Security (RLS) policies within the SQL migration targeting `auth.uid()` filtering isolation.

## 2. Premium Frontend Architecture

- [x] 2.1 Radically refactor `app/dashboard/page.jsx` to establish the root Advanced Grid layout comprising Overview Cards, the core Subjects Grid, and the new Tasks List block.
- [x] 2.2 Re-engineer the visual CSS applying the "Rich Black" deep theme baseline and complex translucent background parameters honoring AGENTS.md rules.
- [x] 2.3 Build the visual `academic_tasks` array UI component mapped to metallic blue action nodes and Glassmorphism techniques (`backdrop-blur`).
- [x] 2.4 Retrofit the existing fetched subjects display map seamlessly into the new translucent glass card array engine.
